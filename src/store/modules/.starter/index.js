import actions, { types } from './actions';
import getters from './getters';
import mutations from './mutations';

const state = {};

export { types };
export default {
  state,
  actions,
  getters,
  mutations
};
